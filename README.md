# Dell XPS15 9570 system cusotmization

## Touchpad

It might be useful to debug the active trackpad features, to do so run `libinput debug-gui` with privilege.

- To enable `tap-to-click` see `30-touchpad.conf`
